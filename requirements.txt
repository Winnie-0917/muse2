# MUSE 2 raw EEG acquisition over BLE
# Install into a virtualenv:  python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
muselsl        # MUSE streaming layer (pulls in pylsl)
bleak          # Native Linux BLE backend (BlueZ / D-Bus), no dongle-specific drivers needed
numpy
